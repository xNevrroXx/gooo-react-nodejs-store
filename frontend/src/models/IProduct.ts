export interface IProduct {
    id: number,
    name: string,
    price: number,
    weight: number,
    shortDescription: string,
    longDescription: string,
    image: string,
    thumb: string,
    categoryId: number,
    location: string,
    stock: number,
    createdAt: string
}